#include "main.h"
#include <stdio.h>

/**
 * _abs - Computes the absolute value of an integer
 * 
 * Return: Always 0 (Success)
 */

int _abs(int)
{
	int c;
	
	r = _abs(c);
	{
		_putchar(r);
	}
	_putchar('\n');
	return (0);
}
