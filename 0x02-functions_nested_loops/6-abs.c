#include "main.h"
#include <stdio.h>

/**
 * _abs - Computes the absolute value of an integer
 * @c: Standard input
 * Return: Always 0 (Success)
 */

int _abs(int c)
{
	int r;

	r = _abs(c);
	{
		printf("%d", r);
	}
	_putchar('\n');
	return (0);
}
