#include "main.h"

/**
 * add - Adds two values together
 * This program use function prototypes to print
 * @a: Standard input
 * @b: Standard input
 * Return: Always 0 (Success)
 */

int add(int a, int b)
{
	int n;

	n = add(a, b);
	{
		_putchar(n + '0');
		_putchar('\n');
	}
	return (0);
}
