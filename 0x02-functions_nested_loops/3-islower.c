#include "main.h"
#include <ctype.h>

/**
 * _islower - Checks if an alphabet is in lower case
 *
 * Return: Always 0 (Success)
 * */

int _islower()
{
	int r;

	if r == _islower
	{
		_putchar('1')
	}
	else
	{
		_putchar('0')
	}
	_putchar('\n')
}
