#include "main.h"
#include <ctype.h>

/**
 * _islower - Checks if an alphabet is in lower case
 * @c: stardard input
 * Return: Always 0 (Success)
 * */

int _islower(int c)
{
	int r;

	if _islower(r)
	{
		_putchar('1');
	}
	else
	{
		_putchar('0');
	}
	_putchar('\n');
	return (0);
}
