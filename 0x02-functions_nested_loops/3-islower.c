#include "main.h"
#include <ctype.h>

/**
 * _islower - Checks if an alphabet is in lower case
 * @c: stardard input
 * Return: Always 0 (Success)
 * */

int _islower(int c)
{

	if (_islower(c))
	{
		_putchar('1');
	}
	else
	{
		_putchar('0');
	}
	_putchar('\n');
	return (0);
}
