#include "main.h"

/**
 * print_sign - Checks if a number is positive or negative
 * @n: stardard input
 * Return: Always 0 (Success)
 */

int print_sign(int n)
{

	if (n > 0)
	{
		return (1);
		_putchar('+');
	}
	if else (n == 0)
	{
		return (0);
		_putchar('0');
	}
	else
	{
		return (-1);
		_putchar(-);
	}
	_putchar('\n');
}
